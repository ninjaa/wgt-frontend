{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "outDir": "../dist/out-tsc",
    "types": ["q", "selenium-webdriver", "jasmine"],
    "mapRoot": "./"
  },
  "exclude": [
    "test.ts",
    "**/*.spec.ts",
    "assets"
  ]
}
